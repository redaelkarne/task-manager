import { TaskStatusColorDirective } from './task-status-color.directive';

describe('TaskStatusColorDirective', () => {
  it('should create an instance', () => {
   // const directive = new TaskStatusColorDirective();
   // expect(directive).toBeTruthy();
  });
});
